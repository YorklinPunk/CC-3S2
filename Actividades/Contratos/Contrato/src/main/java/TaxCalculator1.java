public class TaxCalculator1 {
    public double calculateTax(double value) {
        //POST CONDICION value < 0
        int taxValue = -1;

        assert value >= 0 : "Valor no puede ser negativo";
        assert taxValue >= 0 : "taxValue no puede ser negativo";
        //taxValue
        return taxValue;

    }
}
